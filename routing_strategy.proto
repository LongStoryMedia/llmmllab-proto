// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package models;

option go_package = "github.com/example/models";

// Strongly typed routing strategy result containing workflow selection and execution details
message RoutingStrategy {
  // List of workflow names selected for execution
  repeated string selected_workflows = 1;
  // Strategy for executing the selected workflows
  enum ExecutionStrategyEnum {
    EXECUTIONSTRATEGYENUM_UNSPECIFIED = 0;
    EXECUTIONSTRATEGYENUM_SINGLE = 1;
    EXECUTIONSTRATEGYENUM_PARALLEL = 2;
    EXECUTIONSTRATEGYENUM_SERIES = 3;
    EXECUTIONSTRATEGYENUM_HYBRID = 4;
  }
  

  // Strategy for executing the selected workflows
  ExecutionStrategyEnum execution_strategy = 2;
  // Explanation for why this routing strategy was chosen
  enum ReasonEnum {
    REASONENUM_UNSPECIFIED = 0;
    REASONENUM_COMMAND_BASED_ROUTING = 1;
    REASONENUM_EXPLICIT_ROUTING_DECISION = 2;
    REASONENUM_EXPLICIT_WORKFLOW_TYPE = 3;
    REASONENUM_COMPLEX_RESEARCH_SERIES = 4;
    REASONENUM_MULTI_AGENT_INTENT = 5;
    REASONENUM_CREATIVE_INTENT = 6;
    REASONENUM_RESEARCH_INTENT = 7;
    REASONENUM_DEFAULT_CHAT = 8;
    REASONENUM_ERROR_FALLBACK = 9;
  }
  

  // Explanation for why this routing strategy was chosen
  ReasonEnum reason = 3;
  // Confidence score for the routing decision (0.0 to 1.0)
  double confidence = 4;
  // Additional metadata about the routing decision
  message Metadata {
  message Metadata {
    // Identified primary workflow type from intent analysis
    WorkflowType workflow_type = 1;
    // Assessed complexity level
    enum ComplexityLevelEnum {
      COMPLEXITYLEVELENUM_UNSPECIFIED = 0;
      COMPLEXITYLEVELENUM_SIMPLE = 1;
      COMPLEXITYLEVELENUM_MEDIUM = 2;
      COMPLEXITYLEVELENUM_HIGH = 3;
    }
    
  
    // Assessed complexity level
    ComplexityLevelEnum complexity_level = 2;
    // Whether fallback routing was applied
    bool fallback_applied = 3;
    // Path of routing decisions taken
    repeated string routing_path = 4;
    // Additional properties as key-value pairs
    map<string, string> additional_properties = 5;
  }
  

  // Additional metadata about the routing decision
  Metadata metadata = 5;
  // Target node name for LangGraph conditional routing
  string target_node = 6;
  // Alternative routing strategies that were considered
  repeated AlternativeStrategy alternatives = 7;
}

// Alternative routing strategy that was considered
message AlternativeStrategy {
  repeated string workflows = 1;
  string strategy = 2;
  string reason = 3;
  double confidence = 4;
}
