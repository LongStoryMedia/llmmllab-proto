// GENERATED CODE - DO NOT EDIT
// Generated by schema2code proto generator

syntax = "proto3";

package messages;

option go_package = "github.com/example/messages";

import "document.proto";
import "google/protobuf/timestamp.proto";
import "intent_analysis.proto";
import "message_content.proto";
import "message_role.proto";
import "thought.proto";
import "tool_call.proto";

// Message represents a single exchange in the conversation
message Message {
  // Role of the message sender (e.g., user, assistant)
  MessageRole role = 1;
  // Content of the message
  repeated MessageContent content = 2;
  // Unique identifier for the message
  int32 id = 3;
  // Timestamp of when the message was created
  google.protobuf.Timestamp created_at = 4;
  // Unique identifier for the conversation
  int32 conversation_id = 5;
  // Array of thoughts or reasoning processes from the assistant
  repeated Thought thoughts = 6;
  // Array of tool calls associated with the message
  repeated ToolCall tool_calls = 7;
  // Array of intent analyses associated with the message
  repeated IntentAnalysis analyses = 8;
  // Array of documents attached to the message
  repeated Document documents = 9;
  // Structured output data from the message
  message StructuredOutput {
    // This message intentionally left empty or will be extended later
    reserved 1;
  }
  

  // Structured output data from the message
  StructuredOutput structured_output = 10;
}
